name: Test the Lab 1 of Code changed
on: 
  push:
  workflow_dispatch:
    inputs:
      say:
        description: 'Message to be displayed by the cow'
        required: true
        default: 'moooow'
      eyes:
        description: 'Eye style of the cow'
        required: false
        default: 'O-O'
jobs:
  test-syntax-lab1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # - name: default run
      #   uses: ./.

      - name: custom run with mandatory input
        uses: ./.
        with:
          say: ${{ inputs.say }}

      - name: custom run with all inputs
        uses: ./.
        with:
          say: ${{ inputs.say }}
          eyes: ${{ inputs.eyes }}
      